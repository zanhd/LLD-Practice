package Vehicles.Types;

import DriveStrategy.DriveStrategy;
import Vehicles.Vehicle;

public class SportVehicle extends Vehicle {
    public SportVehicle(DriveStrategy strategy) {
        super(strategy);
    }
}
